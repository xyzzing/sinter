# Environments & dependencies
venv/
.venv/
node_modules/
__pycache__/
*.pyc
dist/
build/

# Local cache & secrets
.env
*.log
.dsh/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
